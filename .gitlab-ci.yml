image: fahad471/glwnd-x:latest

# Define stages for the CI pipeline
stages:
   - build
  # - test

# tags:
#   - gpu
#   - linux




  build:
    tags:
      - gpu
      - linux
    stage: build
    script:
      - cmake --version
      - nvcc --version
      - nvidia-smi
      - mkdir build
      - cd build
      - cmake ..
      - make
  # artifacts:
  #   paths:
  #   - build/
  #   expire_in: 1 hour

  # test:
  #   stage: test
  #   script:
  #     - cp ./benchmark_data.vtk ./build/bin/benchmark_data.vtk
  #     - build/bin/Interactive-LBM-GPGPU test
  